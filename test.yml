version: '3.3'
volumes:
  test:
    driver: haibinfx/goofys:2.0
    driver_opts:
      bucket: "fx-backup-media:gorman"
      gid: 50
      uid: 1000
      debug-fuse:  1
      debug-s3:    1
services:
  test:
    image: alpine
    volumes:
      - test:/mnt/s3



